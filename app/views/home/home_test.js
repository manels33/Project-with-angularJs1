'use strict';

describe('ekk.home module', function() {

  beforeEach(module('ekk.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('homeCtrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});